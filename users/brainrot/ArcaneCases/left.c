// TODO don't forget to update this should i change the base layer
// outer column
case KC_X: {
    ARCANE_STRING("x", KC_X);
} break;
case KC_J: {
    ARCANE_STRING("j", KC_J);
} break;

// pinky column
case KC_V: {
    ARCANE_STRING("v", KC_V);
} break;
case KC_K: {
    ARCANE_STRING("k", KC_K);
} break;
case KC_W: {
    ARCANE_STRING("w", KC_W);
} break;

// ring column
case KC_G: {
    ARCANE_STRING("g", KC_G);
} break;
case KC_S: {
    ARCANE_STRING("s", KC_S);
} break;
case KC_F: {
    ARCANE_STRING("f", KC_F);
} break;

// middle column
case KC_M: {
    ARCANE_STRING("m", KC_M);
} break;
case KC_N: {
    ARCANE_STRING("n", KC_N);
} break;
case KC_L: {
    ARCANE_STRING("l", KC_L);
} break;

// right index column to avoid sfb
case KC_U: {
    ARCANE_STRING("u", KC_U);
} break;
case KC_A: {
    ARCANE_STRING("a", KC_A);
} break;

// thumb
case KC_R: {
    ARCANE_STRING("r", KC_R);
} break;
